function flipCard(card) { /*Defines a function that gets triggered when a card is clicked.*/
    card.classList.toggle("flip"); 
    
    /*If "flip" is NOT present, it adds the "flip" class (causing the card to flip).
If "flip" is already present, it removes the "flip" class (causing the card to flip back).*/
}